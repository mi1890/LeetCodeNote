/**
 * @param {number} n
 * @return {string[]}
 */
var generateParenthesis = function(n) 
{
    var ret = new Array();
    if(n == 0)
    {
        return ret;
    }
    var leftcounter = n-1;
    var rightcounter = n;
    while(true)
    {
                
    }
    return ret;
};